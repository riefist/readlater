"use client";

import { atom } from "jotai";

export const isEditMode = atom(false);
export const bookmarkDataId = atom("");
export const bookmarkTitle = atom("");
export const bookmarkDescription = atom("");
export const bookmarkUrl = atom("");
